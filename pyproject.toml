[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "devmentor-ai"
version = "0.1.0"
description = "Privacy-first ML-powered debugging assistant"
requires-python = ">=3.11"

[tool.setuptools]
packages = ["client", "local_service", "sanitizer", "ml_engine"]